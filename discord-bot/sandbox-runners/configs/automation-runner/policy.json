{
  "allowedSkills": ["automation-workflows"],
  "actions": {
    "automation-workflows": {
      "run": {
        "enabled": true,
        "requiredFields": ["workflowId"],
        "allowedWorkflowIds": ["smoke-test"],
        "maxPayloadKeys": 10
      }
    }
  },
  "network": {
    "egressMode": "allowlist",
    "notes": "Enforce with host firewall/proxy."
  }
}

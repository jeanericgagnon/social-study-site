# Sys changelog (local)

All notable local-only changes Sys makes should be logged here.

## 2026-02-09
- Initialized local docs + vault structure (`docs/` and `vault/`).
- Added Docs index and operation rules.

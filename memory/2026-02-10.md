# 2026-02-10

- Diagnosed Discord billing warning context: current active runs (including `discord-wedding-site` channel) are using **openai-codex / gpt-5.3-codex via OAuth**, not API-key billing path.
- Gateway log check during live troubleshooting showed healthy executions on OAuth route; likely causes for displayed billing warning were an older/stale error message or another bot/integration source in channel.
- Verification step established: send a fresh mention (e.g., "ping now") in target channel to confirm live routing health when warning appears.
- Domain shortlist note: Eric selected **BookedByLove** as a preferred naming direction; `bookedbylove.com` checked as **likely available** at time of lookup.
- Additional domain preference saved: **bookedby.love** (Eric likes this direction).
- Discord project channels: created/using dedicated `#i-would-vouch` channel session for isolated build context (separate from `#wedding-site`).
- Execution quality preference: Eric wants build bots monitored for **proof-of-work updates only** (commit hash + files changed + build/lint/test + preview URL), to avoid status-loop chatter.
- Discord permissions note: bot lacks `Manage Channels` for moving channels into category (channel edit/move returned `Missing Permissions`), so Eric must move channels manually or grant permission.

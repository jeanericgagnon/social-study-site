## 2026-02-28 14:32 PST — Gateway lag triage + compaction tuning
- Symptom: `wedding-site-pm` bot was lagging/not responding despite WhatsApp gateway reconnects.
- Findings from logs/status:
  - Gateway restart worked and services came back healthy.
  - Discord event processing showed timeout: `DiscordMessageListener timed out after 30000ms`.
  - `discord-project-manager` session was near context ceiling (~251k / 272k tokens).
- Actions taken:
  - Restarted gateway multiple times during incident validation.
  - Tuned `~/.openclaw/openclaw.json` compaction settings under `agents.defaults.compaction`:
    - `reserveTokens: 45000`
    - `keepRecentTokens: 16000`
    - `reserveTokensFloor: 24000`
    - `maxHistoryShare: 0.6`
    - `memoryFlush.enabled: true`
    - `memoryFlush.softThresholdTokens: 180000`
  - Reduced Discord agent `groupChat.historyLimit` from 40 to 20 for all `discord-*` agents.
  - Ran `openclaw doctor --fix` (applied discord dm config migration cleanup).
  - Ran `openclaw sessions cleanup --all-agents --enforce --json` (no entries pruned/capped).
  - Verified gateway healthy after restart (`RPC probe: ok`).
- Notable side signal:
  - During restart/check, saw: `missing tool result in session history; inserted synthetic error result for transcript repair`.

# 2026-03-03

- Eric defined a new PM/executor workflow for dayof.love:
  - Planning + decisions happen with Sys in `#dayoflove-pm`.
  - Sys dispatches Gilfoyle tasks in Discord channel `erlich-response`.
  - Gilfoyle reports completion/blockers by @mentioning Sys in `gilfoyle-response`.
  - Execution should run in 3–5 minute batches.
  - If a task reaches 10 runs and is still not done, Sys must report back to Eric in PM before continuing.
- Eric asked for a practice run: QA then smoke test in 2 batches; Sys dispatched Batch 1 (`PRACTICE-QA-01`) to `erlich-response` and is awaiting Gilfoyle response.
- Eric requested a tool search for pulling video transcripts from URL; Sys identified candidate skills and flagged `transcribee` as caution until source audit.
- Sys completed a safety audit for `itsfabioroma/transcribee` (installed from Skills registry): quick static review + dependency audit (`npm audit --omit=dev`) with no known npm vulns at scan time.
- Key risk documented: `transcribee` sends media/transcript data to third-party APIs (ElevenLabs STT + Anthropic classification), so it is not local-only privacy safe.
- Sys sandboxed usage by adding:
  - `sandbox-runners/transcribee/run-safe.sh` (approval gate + URL allowlist + local-file opt-in + env-key checks)
  - `sandbox-runners/transcribee/SECURITY-POLICY.md`
  - shared reference doc at `~/Desktop/OPENCLAW_TOOL_SAFETY_REFERENCE.md` (workspace copy: `exports/OPENCLAW_TOOL_SAFETY_REFERENCE.md`).
- Sys updated `TOOLS.md` with transcribee sandbox references and committed changes in workspace git:
  - commit `c20379e` — “Add sandboxed transcribee wrapper and shared tool safety reference”.
- In `#wedding-site-chat`, Eric asked Sys to craft Bolt.new prompts to generate high-quality wedding templates with strong vibe/color differentiation.
- Sys reviewed repo structure expectations and emphasized integration constraints for template generation:
  - sectionized architecture (no monolithic page templates)
  - compatibility with `src/templates/registry.ts`, `src/sections/sectionRegistry.tsx`, and `src/types/layoutConfig.ts`
  - valid section types/variants only, reusable section composition, and production-safe defaults.
- Eric shared external repo `jeanericgagnon/wedding-tempaltes`; Sys evaluated progress across iterations and identified improvements/final gaps (contract consistency, theme preset ID compatibility, and variant mapping risk vs main production repo).
- Sys delivered reusable “full prompt” + “strict validator prompt” patterns for future Bolt runs to force schema compliance and reduce vibe-only output drift.
- In Discord channel `#the-social-study`, Eric provided core TSS operating context for "Second Brain" memory:
  - Team (3 employees):
    - Nick (ops): speaker interviews, venue coordination, emcee lineup.
    - Quino (customer service + execution): event setup, IG slides, LinkedIn cross-posting, Mailchimp setup for event launches + weekly newsletter, IG comments/DMs, Eventship setup.
    - Madi (social media creator): 2 reels/week.
  - Regions: Denver, San Diego, Orange County.
  - Core links:
    - Ticketing: https://eventship.com/host/the-social-study
    - Instagram: https://www.instagram.com/thesocial.study/?hl=en
    - LinkedIn admin: https://www.linkedin.com/company/109067092/admin/dashboard/
  - Weekly cadence (PST):
    - Sunday 10:00 AM: tickets go live + ticket-drop newsletter.
    - Monday: reel post.
    - Tuesday 10:00 AM: newsletter send.
    - Wednesday ~4:00 PM: IG slides/carousel post.
    - Friday: reel post.
  - Data model clarification: city "Calendar" tabs hold weekly event assignments; street addresses are maintained in a separate Venue Register source.
  - This-week check (from calendar tabs, Mar 2–Mar 8):
    - Denver: Mar 4, speaker Daniel Baker, Station 26 (North Park Hill), title on space weather impacts.
    - San Diego: Mar 4, speaker Eric Mitchell, Societe (Old Town), title on money/tax system.
    - Orange County: no event populated for that week.
- Verified live pull from Google Sheet `Speaker Responses - San Diego` (file id `1QwuvyafuH70TRiMTRsx1sWVq4BQcWjuWdwDzAX0fAg8`) for the Mar 8 ticket drop row:
  - Week: Apr 06 – Apr 12
  - Drop date: March 8th
  - Event date: April 8th
  - Speaker: Claudia Erickson
  - Venue: Societe (Old Town)
  - Title: "How AI can warp our reality - even smart people are falling into this trap and we are seeing cases of AI psychosis and delusions or just losing themselves."
  - Venue register address used: 2415 San Diego Ave, Ste 111, San Diego, CA 92110.
- Cron check confirmed the active pre-event reminder automation:
  - Job name: `eventship:3day-check-email`
  - Job id: `5206407d-618b-443a-bfba-6bba3ef02f78`
  - Schedule: daily 06:00 America/Los_Angeles (`0 6 * * *`)
  - Command: `python3 /Users/ericsysclaw/.openclaw/workspace/scripts/quino_eventship_email.py`
  - Last run status: ok; enabled=true.
- Related duplicate/older job observed and currently disabled:
  - `Quino Eventship 3-day email reminder` (id `479207f7-dcff-4dd5-b688-b5277a58f757`).
- Operational lesson from live test: local `drive-downloads/*.xlsx` snapshots can be stale versus live Google Sheets; when accuracy matters (e.g., "what drops this week"), pull current data directly from Drive/Sheets before answering.
- Discord ops update: Eric asked to move `#dayoflove-dev` into `Wedding Site` category. Initial tool call reported success, but live verification showed channel stayed uncategorized (`parent_id: null`). A direct re-parent attempt returned `Missing Permissions`, so the current blocker is Discord permissions for this bot (needs ability to manage/move channels).
- Multi-bot behavior tuning: mention-only gating in `#dayoflove-pm`/devtank-style channel caused bots to type without posting (likely falling through to `NO_REPLY`). Removing strict mention-only gating restored normal responses for most bots.
- Transcription workflow note: transcribee run-safe wrapper correctly enforced missing key checks (`ELEVEN_LABS_API_KEY`, `ANTHROPIC_API_KEY`) and blocked execution without env vars, even after user approval. As fallback, Sys extracted transcript from YouTube’s on-page transcript panel via browser automation.
- Recommendation captured: set up local Whisper + yt-dlp + ffmpeg for better, repeatable URL transcription quality without third-party key dependency in this runtime.
- In `#dayoflove-dev`, Eric confirmed a Builder-tab migration strategy for `wedding-site-Bolt`:
  - `wedding-site-Bolt` is the main production site/repo.
  - Preserve all existing functionality during the redesign.
  - Scope is Builder tab only.
  - Apply `wedding-site-builder` UI/UX patterns as reference.
  - Keep current Bolt templates/variants as canonical data source (do **not** replace with builder repo templates).
- Working cadence preference captured from Eric for execution: ship in short 3–5 minute batches with explicit `continue` handoffs; stay silent between batch completion and next `continue`; finish with QA + smoke test phase.
- Sys created migration planning doc: `wedding-site-bolt-builder-migration-plan.md` with locked scope, non-negotiables, and parity acceptance checklist.
- Sys cloned `jeanericgagnon/wedding-site-Bolt` into workspace and completed Batch 1 inventory/diff mapping:
  - Bolt builder entrypoints mapped (`BuilderPage`, `BuilderShell`, `TemplateGalleryPanel`, state/services/adapters).
  - `wedding-site-builder` reference components identified for UI mapping (`BuilderApp`, `TemplateGallery`, `VariantGallery`, `VariantPreviewModal`, cards).
  - Integration strategy locked: UI-first swap with adapter boundaries only; no backend/data contract changes in UI batches.

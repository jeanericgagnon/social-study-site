# 2026-03-09

## Model/Auth setup completed
- Installed Claude Code CLI globally: `@anthropic-ai/claude-code`.
- Completed `claude setup-token` auth flow and added Anthropic token to OpenClaw via `openclaw models auth setup-token --provider anthropic --yes`.
- Confirmed OpenAI remained primary (`openai-codex/gpt-5.3-codex`) while Claude auth was added.

## Routing/alias policy configured
- Added model aliases in OpenClaw:
  - `fast-code` -> `openai-codex/gpt-5.3-codex`
  - `deep-think` -> initially `anthropic/claude-sonnet-4-5`, later changed to `anthropic/claude-3-7-sonnet-latest` due to allowlist issues in Discord context.
- Fallbacks set to Claude models:
  - `anthropic/claude-3-7-sonnet-latest`
  - `anthropic/claude-sonnet-4-5`

## Discord model behavior troubleshooting
- User reported Discord-side errors: "model not allowed" / allowlist blocks.
- Identified likely bad model string in that path (`anthropic/claude-3.7-sonnet` typo vs valid hyphenated ids).
- Updated per-discord agent model config in `~/.openclaw/openclaw.json` so `discord-*` agents use Claude primary with Codex fallback.
- Restarted gateway multiple times during troubleshooting.

## Shell convenience change
- Added to `~/.zshrc`:
  - `alias claude="claude --model sonnet"`
  (intended to reduce CLI-side model selection errors)

## User intent/preferences reinforced
- User wants automatic model selection and ability for agents to use either provider.
- User prefers that "use Claude" in Discord should reliably choose an allowed Claude model without manual model-id handling.

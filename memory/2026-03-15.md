# 2026-03-15

- User asked why `discord-tss-second-brain` keeps failing; initial hypothesis tied to gateway instability due to repeated WhatsApp reconnect events.
- User requested full WhatsApp removal. Applied config change via gateway:
  - `plugins.entries.whatsapp.enabled = false`
  - `channels.whatsapp.enabled = false`
  - Gateway restart completed successfully at ~13:01 PDT.
- Post-removal validation:
  - `openclaw status` shows channels: Discord ON/OK, iMessage ON/OK, WhatsApp absent/disabled.
  - Recent logs no longer show new WhatsApp reconnect/start events after restart.
- Root cause for ongoing flapping is now Discord-side instability, not WhatsApp:
  - Repeated health-monitor restarts every ~5 minutes for `discord:default`, `discord:michael-scott`, and `discord:tss-second-brain` with reasons `disconnected`/`stale-socket`.
  - Observed Discord WebSocket abnormal close (`code 1006`) and resume attempts.
- Additional finding: multiple unresolved Discord channel IDs appear repeatedly in startup logs; likely config hygiene issue contributing operational noise and possible routing fragility.
- Next proposed remediation (pending user approval): prune unresolved Discord channel entries and tune Discord health-monitor/reconnect behavior.

- In `#the-social-study`, user asked why there were so many Vercel updates and whether 149 deploys in 24h was plausible.
- Verified deploy-noise root cause is multiplicative deploy attempts, not just base cron count:
  - Hourly job `meta-ads-dashboard-refresh-3x-daily` (`6b74c60a-c30f-4b95-a037-8e280c34a26a`) runs every hour.
  - During failure periods, per run could trigger multiple Vercel attempts (historically retries + downstream deploy), causing attempt amplification.
  - Two midnight deep-refresh jobs are both active (`aab7cabe-0752-4908-a4c1-ff71d3733468` and `78892099-27df-4f57-9cbf-857676aad255`), creating duplicate midnight execution paths.
  - Manual/retry deploy activity during incidents contributed additional attempts.
- Evidence observed:
  - `vercel ls` showed dense clusters of `Error` production deploys with intermittent `Ready`.
  - `health-board/logs/deploy.log` includes repeated `api-deployments-free-per-day` errors and many consecutive deploy attempts.
- Current script state noted:
  - `ads-ops/scripts/run_kpi_pull.sh` now states deploys were intentionally removed (pull-only), with publish centralized in `health-board/scripts/sync_and_deploy.sh`; historical run summaries may still reference earlier retry behavior.
- User accepted the quantitative explanation that retry/error bursts + duplicate scheduled paths can reasonably produce ~149 rolling-24h deploy attempts.
